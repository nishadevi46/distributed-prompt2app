spring:
  application:
    name: config-service

  cloud:
    config.server.git:
      uri: https://github.com/nishadevi46/prompt-config-server
      username: nishadevi46
      password: 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
      default-label: main

server.port: 8888

management:
  endpoints:
    web:
      exposure:
        include: health,info
  health:
    config:
      enabled: false
---

spring:
  config:
    activate:
      on-profile: k8s

  cloud:
    discovery:
      enabled: false
    config.server.git:
      uri: https://github.com/nishadevi46/prompt-config-server
      username: ${GIT_USERNAME}
      password: ${GIT_PASSWORD}

eureka:
  client:
    enabled: false
    register-with-eureka: false
    fetch-registry: false